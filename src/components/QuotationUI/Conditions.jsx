import React from "react";
import { Card, Typography } from "antd";

const { Title, Text } = Typography;

const Conditions = () => {
  return (
    <Card size="small">
      <Title level={5}>Conditions</Title>
      <Text>Conditions details will be displayed here.</Text>
    </Card>
  );
};

export default Conditions;
